# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats the code as read-only ground truth and edits only documentation so it reflects the repo's actual current state. It changed no source code, config, tests, or lockfiles, and ran no builds or tests. Its purpose is to provide an auditable record of what was found and what was changed.

---

## Per-Claim Findings

### 1. What it is
**Status:** `consistent`  
**Evidence:** `Wavelength/App/WavelengthApp.swift` (`@main` App struct, no AppDelegate), `Wavelength/Core/SignalRegistry.swift`, `Wavelength/Metal/SpectrogramRenderer.swift`, `README.md`  
The README description ("real-time electromagnetic spectrum visualizer for iOS... 70 MHz to 6 GHz"), architecture paragraph (four sensor actors, SignalRegistry, Metal pipeline), and feature list all accurately describe what is implemented.

### 2. Current state
**Status:** `drifted` → **fixed**  
**Evidence:** `WavelengthTests/**/*.swift` — 89 `@Test` annotations counted across 17 test files. All tests use Swift Testing (`import Testing`); none use XCTest-style `func test*`. All five `@Test` format variants present: `@Test func`, `@MainActor @Test func`, `@Test @MainActor func`.

`CLAUDE.md` Build/Test/Run section and Portfolio Context section, plus `docs/PORTFOLIO-DISPOSITION.md` (3 locations), all said "63 tests" — which matched a stale memory record from a prior session. Counting `@Test` annotations directly in the current source gives 89.

The prior pass (2026-05-30, sha `ab4a1c7`) introduced "56 files, 63 tests" — the 63 was already incorrect at the time the count was taken (89 `@Test` annotations were present). The 56-file count remains plausible (35 Swift source + 1 Metal shader + 17 test Swift + airports.json + Wavelength.entitlements + PrivacyInfo.xcprivacy = 56) and was left unchanged.

**Changes:**
- `CLAUDE.md` line 18 (Build/Test/Run): `63 passing tests` → `89 passing tests`
- `CLAUDE.md` line ~59 (Portfolio Context / Current State): `63 tests` → `89 tests`
- `docs/PORTFOLIO-DISPOSITION.md` line 61: `Phase 3 complete, 63 tests` → `Phases 0–3 complete, 89 tests`
- `docs/PORTFOLIO-DISPOSITION.md` line 170 (Reactivation procedure step 6): `Run XCTest target — 63 tests per memory` → `Run Swift Testing / XCTest target — 89 tests`
- `docs/PORTFOLIO-DISPOSITION.md` line 184 (Last known reference table): `Phase 3 per memory; 63 tests` → `Phases 0–3 complete; 89 tests`

### 3. Stack
**Status:** `consistent`  
**Evidence:** `Wavelength.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved`  
- Alamofire: 5.11.1 — within `~5.9` range ✓
- GRDB.swift: 7.10.0 — within `~7.x` range ✓
- SatelliteKit: 2.1.1 from `https://github.com/gavineadie/SatelliteKit` — within `~2.x` range ✓

Metal texture pixel format `rgba8Unorm` confirmed at `Wavelength/Metal/SpectrogramTexture.swift:24`.

### 4. How to run
**Status:** `consistent`  
**Evidence:** `Wavelength.xcodeproj/project.pbxproj` exists; `Wavelength.xcodeproj/project.xcworkspace/contents.xcworkspacedata` exists. README quick-start (`open Wavelength.xcodeproj`, deploy to physical device) and CLAUDE.md Build/Test/Run section (`xcodebuild test`) match the project structure. No `Makefile` or `package.json` scripts are present — Xcode is the only build path.

### 5. Known risks / constraints
**Status:** `consistent`  
**Evidence:** SPM-only confirmed (no `Podfile`, no `Cartfile`); GRDB for signal storage confirmed (`Wavelength/Database/DatabaseManager.swift`); privacy-mode Bluetooth handling confirmed (`Wavelength/Sensing/BluetoothScanner.swift`); FM stations use bundled SQLite (`Wavelength/Resources/fm_stations.sqlite`, `Wavelength/Contextual/FMStationService.swift:1-26`) — no runtime API call. All "Gotchas" in CLAUDE.md remain accurate.

### 6. Next move
**Status:** `consistent`  
**Evidence:** No TestFlight configuration, App Store Connect submission, or release-tag evidence visible in the repo. `CLAUDE.md` "next: TestFlight beta and App Store submission" and `docs/PORTFOLIO-DISPOSITION.md` "Unblock trigger" items remain accurate. `docs/PORTFOLIO-DISPOSITION.md` "Last known reference" origin/main tip (`99b04ef`) was NOT updated — this is a historical snapshot field recording when the disposition was last verified, not a claim about the current HEAD.

---

## Contradictions for Manual Review

These drift items are in files this pass is not permitted to edit. A human should make these corrections.

| Location | What is wrong | Suggested fix |
|---|---|---|
| `IMPLEMENTATION-ROADMAP.md` System Overview diagram | `RGBA16F circular buffer` | Change to `RGBA8Unorm circular buffer` — verified at `Wavelength/Metal/SpectrogramTexture.swift:24` |
| `IMPLEMENTATION-ROADMAP.md` File Structure | Lists `App/AppDelegate.swift` | Remove — there is no AppDelegate; `WavelengthApp.swift` uses the SwiftUI App protocol directly |
| `IMPLEMENTATION-ROADMAP.md` File Structure | Lists `Database/Migrations/Migration002_fcc_index.swift` | Rename to `Migration002_Onboarding.swift` — that is the actual file |
| `IMPLEMENTATION-ROADMAP.md` File Structure | Lists `WavelengthTests/FCCDatabaseTests.swift` | File does not exist; `WavelengthTests/DatabaseManagerTests.swift` is the closest equivalent |
| `IMPLEMENTATION-ROADMAP.md` File Structure | Lists `Resources/itu_regions.json` | File is absent from the bundle |
| `IMPLEMENTATION-ROADMAP.md` Phase 2 Task 4 | Describes `FMStationService.swift` calling the live FCC FM Query API | Actual implementation reads from bundled `fm_stations.sqlite` — no runtime API call (`FMStationService.swift:1-26`) |
| `IMPLEMENTATION-ROADMAP.md` File Structure | Lists `Sensing/SensingEngine.swift` | No such file; sensor actors are wired directly in `WavelengthApp.swift` |
| `IMPLEMENTATION-ROADMAP.md` Dependencies | Lists `SwiftyTLE` from `https://github.com/SatelliteKit/SwiftyTLE` | Actual resolved package is `SatelliteKit` 2.1.1 from `https://github.com/gavineadie/SatelliteKit` |

---

## Footer

**Timestamp:** 2026-06-02 19:56:58 PDT  
**Branch:** docs/truth-up-2026-06-02  
**HEAD sha reconciled against:** b8c61f454eaec4b6e9702d702cc071024f021d74
